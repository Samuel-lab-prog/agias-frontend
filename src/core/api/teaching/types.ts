export type Page<T> = { items: T[]; total: number; page: number; pageSize: number };
export type TeachingClass = { id: number; title: string; code: string; year: number; shift: string; course: { name: string }; academicPeriod: { code: string }; _count: { enrollments: number; sessions: number; activities: number } };
export type Lesson = { id: number; classOfferingId: number; topic: string | null; startsAt: string; endsAt: string | null; room: string | null; deliveredContent: string | null; publicNotes: string | null; status: string; coursePlanTopicId: number | null; classOffering: { title: string }; materials: Array<{ id: number; title: string; url: string }> };
export type Activity = { id: number; classOfferingId: number; title: string; description: string | null; kind: string; dueAt: string | null; appliesAt: string | null; maxGrade: number | null; weight: number | null; assessmentType: string | null; allowLateSubmissions: boolean; classOffering: { title: string }; _count: { submissions: number } };
export type Plan = { id: number; syllabus: string | null; generalObjectives: string | null; methodology: string | null; assessmentCriteria: string | null; workloadMinutes: number | null; status: string; units: Array<{ id: number; title: string; description: string | null; position: number; topics: Array<{ id: number; title: string; description: string | null; position: number; type: string }> }> };
export type RosterRow = { id: number; name: string; academicId: string; attendance: string | null; submission: { id: number; submittedAt: string | null; grade: number | null; feedback: string | null; attachments: Array<{ id: number; fileName: string; fileUrl: string }> } | null };
export type TeachingOverview = { profile: { registryCode: string | null; title: string | null; workload: number | null; department: { name: string } | null; user: { name: string; email: string } }; classCount: number; students: number; pendingGrades: number; lessons: Lesson[] };
