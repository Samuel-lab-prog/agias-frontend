import { administration } from '@Api/admin/endpoints';
import { Surface } from '@BaseComponents';
import { Box, Heading, HStack, SimpleGrid, Text, VStack } from '@chakra-ui/react';
import { Building2, GraduationCap, ShieldCheck, UserRoundCog, Users } from 'lucide-react';
import { AdminBadge, AdminHeader, AdminLink, AdminSection, AdminStat, AdminState } from '../../components/AdminUI';
import { useAdminQuery } from '../../hooks';

export function Dashboard() {
	const query = useAdminQuery(['overview'], administration.overview);
	const data = query.data;
	return <VStack align='stretch' gap={6}><AdminHeader title='Visão geral da instituição' description='Pessoas, acessos e pendências que precisam da sua atenção.' action={<AdminLink to='/admin/team/new'>Cadastrar equipe</AdminLink>} /><AdminState query={query}>{data && <><SimpleGrid columns={{ base: 1, md: 2, '2xl': 4 }} gap={4}><AdminStat title='Alunos' value={data.roles.student ?? 0} icon={GraduationCap} to='/admin/users?role=student' /><AdminStat title='Professores' value={data.roles.professor ?? 0} icon={Users} to='/admin/team?role=professor' /><AdminStat title='Secretaria' value={data.roles.staff ?? 0} icon={UserRoundCog} to='/admin/team?role=staff' /><AdminStat title='Administradores' value={data.roles.admin ?? 0} icon={ShieldCheck} to='/admin/team?role=admin' /></SimpleGrid><SimpleGrid columns={{ base: 1, xl: 2 }} gap={5}><Surface variant='panel'><AdminSection title='Acompanhar cadastros' description='Abra uma pendência para consultar as pessoas envolvidas.' /><VStack align='stretch' gap={4}>{[{ title: 'Perfis acadêmicos incompletos', count: data.incomplete, to: '/admin/users?issue=missing-profile' }, { title: 'Professores sem turma vinculada', count: data.unassigned, to: '/admin/team?issue=unassigned-professor' }, { title: 'Turmas sem professor', count: data.classesWithoutProfessor, to: '/admin/pending-classes' }].map((item) => <HStack key={item.title} justify='space-between' gap={3} flexWrap='wrap' pb={3} borderBottomWidth='1px' borderColor='border.muted'><Box><Text fontSize='sm' fontWeight='medium'>{item.title}</Text><Text fontSize='xs' color='fg.muted' mt={1}>{item.count} {item.count === 1 ? 'registro' : 'registros'}</Text></Box><AdminLink to={item.to}>Consultar</AdminLink></HStack>)}</VStack></Surface><Surface variant='gradient'><AdminSection title='Situação dos acessos' description='Contas cadastradas, considerando todos os perfis.' /><VStack align='stretch' gap={4}>{[{ label: 'Ativos', value: data.statuses.active ?? 0, key: 'active' }, { label: 'Primeiro acesso pendente', value: data.statuses.pending ?? 0, key: 'pending' }, { label: 'Suspensos', value: data.statuses.suspended ?? 0, key: 'suspended' }, { label: 'Bloqueados', value: data.statuses.blocked ?? 0, key: 'blocked' }].map((item) => <HStack key={item.key} justify='space-between'><AdminLink to={`/admin/users?status=${item.key}`}>{item.label}</AdminLink><AdminBadge>{item.value}</AdminBadge></HStack>)}</VStack></Surface></SimpleGrid><Box><AdminSection title='Rotinas acadêmicas' description='Acesse as ferramentas compartilhadas com a secretaria.' /><SimpleGrid columns={{ base: 1, md: 3 }} gap={4}>{[{ title: 'Turmas e matrículas', description: 'Organize vínculos docentes e matrículas.', to: '/staff/classes' }, { title: 'Cadastro de alunos', description: 'Registre alunos para o primeiro acesso.', to: '/staff/students' }, { title: 'Calendário institucional', description: 'Consulte e organize os eventos acadêmicos.', to: '/staff/academic-calendar' }].map((item) => <Surface key={item.to} variant='panel'><HStack mb={3} color='action.primary'><Building2 size={18} aria-hidden='true' /><Heading as='h3' fontSize='md'>{item.title}</Heading></HStack><Text color='fg.muted' fontSize='sm' mb={4}>{item.description}</Text><AdminLink to={item.to}>Acessar</AdminLink></Surface>)}</SimpleGrid></Box></>}</AdminState></VStack>;
}
